import "Srl/String";
import "Apm";
Apm.importFile("Alusus/Sle", "Srl/Net.alusus");
use Srl;

func test {
    def request: Net.Request(String("https://example.com"));
    request.verbose = false;
    request.get();
    Console.print("Response from example.com:\n%s", request.responseBody.buf);
}

test();
